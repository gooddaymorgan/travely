import React from 'react';
import hero from '../assests/hero.jpg';

const Hero = (props) => (
      <div>
        <img src={hero} alt="#" width="100%" height="auto"/>
          <h1>
            Broaden Your
            Horizons
          </h1>
      </div>

);

export default Hero;