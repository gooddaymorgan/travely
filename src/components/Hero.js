import React from 'react';
// import hero from '../assests/hero.jpg';

const Hero = (props) => (
      <div class="hero-image" >
          <div class="hero-inner">
            <h1>Broaden Your</h1>
            <h1 class="hero-text-secondary">Horizons</h1>
            <button>Learn More</button>
          </div>
      </div>
);

export default Hero;