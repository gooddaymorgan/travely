import React from 'react';

const Header = (props) => (
    <header>
        <a href="#">Travely</a>

        <div>
            <a href="#">Places</a>
            <a href="#">About</a>
            <a href="#">Itineary Builder</a>
        </div>
    </header>
);


export default Header;