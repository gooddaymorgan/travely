import React from 'react';

const Header = (props) => (
    <header>
        <a href="#">Logo</a>

        <div>
            <a href="#">About</a>
            <a href="#">Places</a>
            <a href="#">Booking</a>
            <a href="#">Profile</a>
        </div>
    </header>
);

export default Header;